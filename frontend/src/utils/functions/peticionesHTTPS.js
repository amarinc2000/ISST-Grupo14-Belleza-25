
//FUNCIONES PARA HACER PETICIONES HTTP A LA API REST
// // Esta función se puede usar para realizar peticiones GET, POST, PUT y DELETE a la API de servicios

// La api es la siguiente
const URL_BASE = "https://localhost:8080/api/"; // Cambia esto a la URL de tu API

// Para tabla Servicios
export function peticionesServicio(url, method, body = null) {
    // Definir la URL base de la API

}


// Para tabla Negocios
export function peticionesNegocio(url, method, body = null) {
    // Definir la URL base de la API
}


// Para tabla Trabajadores
export function peticionesTrabajador(url, method, body = null) {
    // Definir la URL base de la API
}


// Para tabla Clientes
export function peticionesCliente(url, method, body = null) {
    // Definir la URL base de la API
}


// Para tabla Reservas
export function peticionesReserva(url, method, body = null) {
    // Definir la URL base de la API
}


// Para tabla Favoritos
export function peticionesFavoritos(url, method, body = null) {
    // Definir la URL base de la API
}


// Para tabla Usuarios
export function peticionesUsuario(url, method, body = null) {
    // Definir la URL base de la API
}

